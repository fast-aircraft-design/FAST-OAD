version: "2"         # required to adjust maintainability checks
checks:
  method-complexity:
    config:
      threshold: 10
exclude_patterns:
  - "**/contest.py"
plugins:
  pylint:
    enabled: true
  sonar-python:
    enabled: true