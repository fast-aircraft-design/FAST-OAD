title = "OAD process with performances as time step integration"

# List of folder paths where user added custom registered OpenMDAO components
module_folders = []

# Input and output files
input_file = "./inputs/in.xml"
output_file = "./outputs/out.xml"

# Definition of OpenMDAO model
[model]
#    nonlinear_solver = "om.NonlinearBlockGS(maxiter=30)"
#    linear_solver = "om.DirectSolver()"

    [model.group.aerodynamics_highspeed]
        id = "fastoad.aerodynamics.highspeed.legacy"
    [model.performance]
        id = "fastoad.performances.mission"
        propulsion_id = "fastoad.wrapper.propulsion.rubber_engine"
        out_file = "./outputs/mission_CeRAS.csv"
        mission_file_path = "../../../mission_files/missionStudy_R5093_PL13608.yml"
        adjust_fuel = false
        is_sizing = false
        initial_iterations = 0
